Show current project status without making changes unless the user asks.

1. Read `STATE.md` and the latest entries in `EXPERIMENTS.md`
2. Summarize: current phase, last experiment outcomes, key metrics, open questions, immediate next steps
3. List relevant artifact paths under `checkpoints/` if they exist
4. Recommend whether the user should run `/continue`, `/run-phase0`, or `/run-phase1`

Do not implement code in this command unless the user explicitly asks to proceed.
