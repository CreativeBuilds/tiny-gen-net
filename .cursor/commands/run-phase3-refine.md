Execute **Phase 3 refine** (expanded arch diversity + conditioned eval). Follow `experiments/phase3_conditioned_refine.py`.

```bash
python experiments/phase3_conditioned_refine.py --num_gen 80 --num_eval 50
```

After running: log to `EXPERIMENTS.md`, update `STATE.md`.
