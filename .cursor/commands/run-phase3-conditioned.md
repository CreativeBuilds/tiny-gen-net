Execute **Phase 3 conditioned** weight generation. Follow `experiments/phase3_conditioned.py`.

```bash
python experiments/phase3_conditioned.py --cond_train_steps 2000 --num_samples 30
```

After running: log to `EXPERIMENTS.md`, update `STATE.md`.
